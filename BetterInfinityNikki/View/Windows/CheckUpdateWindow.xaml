<ui:FluentWindow x:Class="BetterInfinityNikki.View.Windows.CheckUpdateWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:BetterInfinityNikki.View.Windows"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 x:Name="app"
                 Title="发现新版本"
                 Width="640"
                 MinHeight="200"
                 SizeToContent="Height"
                 Background="#202020"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 mc:Ignorable="d">
    <Grid>
        <Grid Margin="0,48,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 顶部：版本信息 -->
            <StackPanel Grid.Row="0" Margin="24,16,24,8">
                <TextBlock x:Name="VersionTitleText"
                           FontSize="20"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock x:Name="CurrentVersionText"
                           Margin="0,6,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- 中部：更新日志（滚动区域） -->
            <Border Grid.Row="1"
                    Margin="24,0,24,16"
                    Padding="12"
                    MaxHeight="320"
                    CornerRadius="6"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ReleaseNotesText"
                               TextWrapping="Wrap"
                               FontFamily="Consolas, Microsoft YaHei"
                               FontSize="13"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </ScrollViewer>
            </Border>

            <!-- 底部：按钮区域 -->
            <Grid Grid.Row="2" Margin="16,0,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:Button x:Name="ManualDownloadButton"
                           Grid.Column="1"
                           MinWidth="110"
                           Margin="8,0"
                           Appearance="Primary"
                           Icon="{ui:SymbolIcon Globe24}"
                           Content="手动下载"
                           Click="ManualDownloadButton_Click" />

                <ui:Button x:Name="IgnoreButton"
                           Grid.Column="2"
                           MinWidth="110"
                           Margin="8,0"
                           Content="不再提示"
                           Click="IgnoreButton_Click" />

                <ui:Button Grid.Column="3"
                           MinWidth="110"
                           Margin="8,0"
                           Content="取消"
                           Click="CancelButton_Click" />

                <ui:Button x:Name="UpdateButton"
                           Grid.Column="0"
                           MinWidth="130"
                           Margin="0,0,8,0"
                           Appearance="Success"
                           Icon="{ui:SymbolIcon ArrowDownload24}"
                           Content="立即更新"
                           Click="UpdateButton_Click" />
            </Grid>
        </Grid>

        <ui:TitleBar Title="{Binding Title, ElementName=app}"
                     ShowMaximize="False"
                     ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
    </Grid>
</ui:FluentWindow>
